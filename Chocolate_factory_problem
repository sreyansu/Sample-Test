def moveZeros(arr):
    count = 0  # Tracks position of non-zero elements
    for i in range(len(arr)):
        if arr[i] != 0:
            arr[count], arr[i] = arr[i], arr[count]
            count += 1
    return arr

n = list(map(int,input().split()))
print(moveZeros(n))
